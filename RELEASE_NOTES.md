Visto 1.1.7

- 修复阅读器中文分页断点质量问题：分页器改用 Android `StaticLayout` / `TextPaint` 进行行测量，替代旧的 `Paint.breakText()` 手工估算，减少与实际显示的换行差异。
- 优化中文段落页尾断点：避免页面结尾停在 `老姐，` 这类中文逗号/顿号等弱标点后面，也避免把同一段很短的尾巴单独丢到下一页。
- 限制分页回退幅度：弱标点/短尾修正最多回退 2 行，并且在自然页面至少 2 行时保持修正后页面不少于 2 行，避免为了避开坏断点产生一行页。
- 内置字体分页测量与显示对齐：Sans/Serif 字体切换时，分页器使用对应 Android `Typeface` 参与测量，降低字体字宽差异导致的断页偏移。
- 对齐分页和 Compose 显示的文字盒模型：阅读正文显示关闭额外 font padding，使其更接近分页侧 `StaticLayout.setIncludePad(false)` 的测量结果。
- 新增回归测试，覆盖用户反馈的中文段落场景，确保分页不会在 `老姐，` 后断页且全文不丢字、不重字。

## 版本

- 应用版本：1.1.7，Android versionCode 为 28。
