Visto 1.1.8

- 调整阅读器分页策略：取消页尾弱标点、短尾段、新段落等主动回退规则，改为每页按实际可显示高度尽量放满完整行。
- 统一分页高度和正文显示区域：移除额外 `PageEndClearance`，底部页脚/进度条避让只由正文 bottom padding 统一负责，减少页底无意义空白。
- 强化书籍缓存：自动丢弃空缓存、非法 UTF-8 缓存、空白文本缓存和明显乱码缓存，避免再次打开书架书籍时卡在加载中。
- 强化阅读器打开流程：书架和文件浏览器打开书籍时，章节解析/初始分页走统一安全流程；异常卡住时会落到错误态，不再无限 loading。
- 新增/更新回归测试，覆盖分页放满、新段落页底、缓存失效重下和布局高度一致性。

## 版本

- 应用版本：1.1.8，Android versionCode 为 29。
