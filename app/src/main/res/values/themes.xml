<resources>
    <!-- Initial light theme before Compose applies the runtime theme. -->
    <style name="Theme.Visto" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowBackground">@color/app_startup_background</item>
        <item name="android:statusBarColor">@color/app_startup_background</item>
        <item name="android:navigationBarColor">@color/app_startup_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
